// eslint-disable-next-line node/no-unpublished-import

const serverUrl = 'http://localhost:4000';

describe('API tests 1', () => {
  test('Get all records with a GET api/users request', async () => {});
});
